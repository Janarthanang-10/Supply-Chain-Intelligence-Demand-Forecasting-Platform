# Notebooks

This folder is for optional exploratory analysis notebooks (e.g. Jupyter).
The main dashboard (`app.py`) does not depend on anything in this folder —
it's provided as a convenient place to prototype new charts or models
before adding them to `src/`.
